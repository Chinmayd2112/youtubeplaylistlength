// let link=document.getElementById("playlist");

// console.log(link);
if (typeof window === "object") {
    // code is running in a browser environment
    console.log("brow");
  } else {
    console.log("non-bro");
    // code is running in a non-browser environment
  }